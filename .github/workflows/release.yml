name: Build and Release

on:
  push:
    tags:
      - 'justflow-v*.*.*'
      - 'justflow-v*.*.*-beta.*'
      - 'justflow-v*.*.*-alpha.*'
      - 'justflow-v*.*.*-rc.*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set release version
        id: version
        run: echo "value=${GITHUB_REF_NAME#justflow-}" >> "$GITHUB_OUTPUT"

      - name: Build Frontend Docker Image (Stable)
        if: ${{ !contains(github.ref_name, '-beta') && !contains(github.ref_name, '-alpha') && !contains(github.ref_name, '-rc') }}
        run: docker build apps/frontend --file apps/frontend/Dockerfile --tag ghcr.io/justlabv1/justflow:frontend-latest --tag ghcr.io/justlabv1/justflow:frontend-${{ github.sha }} --tag ghcr.io/justlabv1/justflow:frontend-${{ steps.version.outputs.value }}

      - name: Build Frontend Docker Image (Pre-release)
        if: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-rc') }}
        run: docker build apps/frontend --file apps/frontend/Dockerfile --tag ghcr.io/justlabv1/justflow:frontend-beta --tag ghcr.io/justlabv1/justflow:frontend-${{ github.sha }} --tag ghcr.io/justlabv1/justflow:frontend-${{ steps.version.outputs.value }}

      - name: Build Backend Docker Image (Stable)
        if: ${{ !contains(github.ref_name, '-beta') && !contains(github.ref_name, '-alpha') && !contains(github.ref_name, '-rc') }}
        run: docker build apps/backend --file apps/backend/Dockerfile --tag ghcr.io/justlabv1/justflow:backend-latest --tag ghcr.io/justlabv1/justflow:backend-${{ github.sha }} --tag ghcr.io/justlabv1/justflow:backend-${{ steps.version.outputs.value }}

      - name: Build Backend Docker Image (Pre-release)
        if: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-rc') }}
        run: docker build apps/backend --file apps/backend/Dockerfile --tag ghcr.io/justlabv1/justflow:backend-beta --tag ghcr.io/justlabv1/justflow:backend-${{ github.sha }} --tag ghcr.io/justlabv1/justflow:backend-${{ steps.version.outputs.value }}

      - name: Build justflow Docker Image (Stable)
        if: ${{ !contains(github.ref_name, '-beta') && !contains(github.ref_name, '-alpha') && !contains(github.ref_name, '-rc') }}
        run: docker build . --file Dockerfile --tag ghcr.io/justlabv1/justflow:latest --tag ghcr.io/justlabv1/justflow:${{ github.sha }} --tag ghcr.io/justlabv1/justflow:${{ steps.version.outputs.value }}

      - name: Build justflow Docker Image (Pre-release)
        if: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-rc') }}
        run: docker build . --file Dockerfile --tag ghcr.io/justlabv1/justflow:beta --tag ghcr.io/justlabv1/justflow:${{ github.sha }} --tag ghcr.io/justlabv1/justflow:${{ steps.version.outputs.value }}

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Push Docker Images (Stable)
        if: ${{ !contains(github.ref_name, '-beta') && !contains(github.ref_name, '-alpha') && !contains(github.ref_name, '-rc') }}
        run: |
          docker push ghcr.io/justlabv1/justflow:frontend-latest
          docker push ghcr.io/justlabv1/justflow:frontend-${{ github.sha }}
          docker push ghcr.io/justlabv1/justflow:frontend-${{ steps.version.outputs.value }}
          docker push ghcr.io/justlabv1/justflow:backend-latest
          docker push ghcr.io/justlabv1/justflow:backend-${{ github.sha }}
          docker push ghcr.io/justlabv1/justflow:backend-${{ steps.version.outputs.value }}
          docker push ghcr.io/justlabv1/justflow:latest
          docker push ghcr.io/justlabv1/justflow:${{ github.sha }}
          docker push ghcr.io/justlabv1/justflow:${{ steps.version.outputs.value }}

      - name: Push Docker Images (Pre-release)
        if: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-rc') }}
        run: |
          docker push ghcr.io/justlabv1/justflow:frontend-beta
          docker push ghcr.io/justlabv1/justflow:frontend-${{ github.sha }}
          docker push ghcr.io/justlabv1/justflow:frontend-${{ steps.version.outputs.value }}
          docker push ghcr.io/justlabv1/justflow:backend-beta
          docker push ghcr.io/justlabv1/justflow:backend-${{ github.sha }}
          docker push ghcr.io/justlabv1/justflow:backend-${{ steps.version.outputs.value }}
          docker push ghcr.io/justlabv1/justflow:beta
          docker push ghcr.io/justlabv1/justflow:${{ github.sha }}
          docker push ghcr.io/justlabv1/justflow:${{ steps.version.outputs.value }}

  release:
    runs-on: ubuntu-latest
    permissions: write-all
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1
        with:
          name: ${{ contains(github.ref_name, '-beta') && format('🚧 Beta Release {0}', github.ref_name) || contains(github.ref_name, '-alpha') && format('🔬 Alpha Release {0}', github.ref_name) || contains(github.ref_name, '-rc') && format('🎯 Release Candidate {0}', github.ref_name) || format('🚀 Release {0}', github.ref_name) }}
          tag: ${{ github.ref_name }}
          skipIfReleaseExists: true
          generateReleaseNotes: true
          prerelease: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-rc') }}
          bodyFile: release-notes.md
